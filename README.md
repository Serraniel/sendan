# Screenshots

Images referenced from pull request descriptions, kept on a branch of their own
so that no binary lands in `main` or in a merge.

Nothing here is built, shipped, or checked by any workflow. A directory may be
deleted once its pull request is closed.
